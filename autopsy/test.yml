---
# possibly saved as tasks/foo.yml

#- name: placeholder foo
#  debug:
#    msg: "{{ my_msg }}/bin/foo"

#- name: placeholder bar
#  debug:
#    msg: /bin/bar

- name: Exists
  stat:
    path: "{{ creates }}"
  register: result

- name: Skipping
  debug:
    msg: "Skipping {{ creates }} already exists"
  when: not result.stat.exists


#- name: Debug
#  debug:
#    msg: "Ansible When File Exists Example."
#  when: not result.stat.exists

- name: Download
  get_url:
    url: "{{ url }}"
    dest: "{{ dest }}"
  when: not result.stat.exists
#    mode: "{{ item.mode | default('0440') }}"
