#!/usr/bin/env bash

function update_resolve
{
  echo "domain=${1}" >> /etc/resolv.conf
}

for item in ${1[@]}
do
  echo "Checking ${item}"
  egrep "domain=${item}" /etc/resolv.conf ||  update_resolve "${item}"
done